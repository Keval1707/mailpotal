<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Submit Details</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    /* Global styles */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    /* Container styles */
    .container {
      background-color: #fff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      width: 400px;
      max-width: 90%;
    }

    /* Heading styles */
    h1, h2 {
      text-align: center;
      color: #333;
    }

    /* Form styles */
    form {
      display: flex;
      flex-direction: column;
    }

    label {
      font-weight: bold;
      margin-bottom: 8px;
      color: #555;
    }

    textarea {
      padding: 12px;
      font-size: 16px;
      border: 1px solid #ddd;
      border-radius: 6px;
      resize: vertical;
    }

    button {
      background-color: #4CAF50;
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 16px;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #45a049;
    }

    /* Icon styles */
    .icon {
      margin-right: 8px;
    }

    /* Responsive design */
    @media (max-width: 600px) {
      .container {
        width: 90%;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><i class="fas fa-envelope"></i> Submit Your Details</h1>
    <form action="/submit-form" method="post">
      <label for="emails"><i class="fas fa-at icon"></i>Emails (one per line or comma-separated):</label>
      <textarea id="emails" name="emails" placeholder="Enter multiple email addresses, one per line or separated by commas" required rows="5"></textarea>
      <br><br>
      <label for="message"><i class="fas fa-comment icon"></i>Message:</label>
      <textarea id="message" name="message" placeholder="Enter your message" required rows="5"></textarea>
      <br><br>
      <button type="submit"><i class="fas fa-paper-plane icon"></i> Submit</button>
    </form>
  </div>
</body>
</html>
